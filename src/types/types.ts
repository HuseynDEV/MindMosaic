export type userSignUpType = {
    username: string,
    email: string,
    password: string,
    // displayName?: string
}

export type userSignInType={
    email:string,
    password:string
}